language: php
php:
  - 7.4
services:
  - mysql
   
before_script:
  - composer install 
  - mysql -e 'CREATE DATABASE db;'
  - mysql db < sql/database.sql 
  
  
script: phpunit --configuration travis.xml
